import { Container, Typography } from '@mui/material';

export const ContulMeu = () => {
  return (
    <Container
      maxWidth="lg"
      sx={{
        height: '100%',
      }}
    >
      ContulMeu
      <Typography variant="h2">ContulMeu</Typography>
    </Container>
  );
};
